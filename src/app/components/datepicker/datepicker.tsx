import BaseInput from "../input/text/BaseInput";

const datepicker = () => {
  return <BaseInput name="datepicker" type="text" placeholder="날짜를 선택해주세요." variant="white" />;
};

export default datepicker;
